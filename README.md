# API-redis-writer
A test API integrated into the Laniakea Dashboard that authenticates users via OIDC and writes deployment jobs to a Redis queue, where they are consumed by a dedicated worker agent.
